apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes-app-blueprint.fullname" . }}
  labels:
    {{- include "kubernetes-app-blueprint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubernetes-app-blueprint.selectorLabels" . | nindent 4 }}
